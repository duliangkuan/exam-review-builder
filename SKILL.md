---
name: exam-review-builder
description: 基于用户提供的教材、课件、考试范围和真题，制作有来源标注的期末复习资料包；当用户要整理课程重点、生成零基础讲义、题库、离线交互复习页或打印版时使用。不要用于无资料依据的押题、代写正在进行的考试，或把通用知识冒充指定教材结论。
version: "1.0.0"
author: 风云
---

# 期末复习资料生成器

把用户有权使用的课程材料整理成可核查、可离线使用的复习资料。核心不是“写得像教材”，而是让每个重要结论能回到原材料，让学生知道先学什么、怎么解题、哪里容易错。

## 适用边界

正向触发：

- “根据这本教材和考试范围做一套期末复习资料。”
- “分析这些历年卷，整理高频考点、题型模板和题库。”
- “把现有复习 Markdown 做成可离线答题和打印的讲义。”

反向触发：

- 只需要生成一张新试卷时，使用试卷生成类 Skill。
- 只需要旋转、合并或提取 PDF 时，使用 PDF 工具。
- 没有课程材料却要求预测原题、保证分数或声称“内部押题”时，不执行该承诺；可改做公开范围内的通用复习计划。
- 不帮助代答正在进行的闭卷考试，也不绕过付费、登录或访问控制获取资料。

## 输入

至少需要一种课程依据：教材、讲义、课件、课程大纲、教师划定范围或合法取得的历年题。优先同时收集科目名、考试日期、可投入时间、学生基础、考试范围与题型、教材版本和页码体系、历年题答案可信度，以及期望产物。

缺少的信息会改变结论时先询问；只影响展示细节时采用保守默认值并说明。

## 输出

默认建立一个科目目录并产出：

1. `复习资料.md`：完整讲义，含来源标注、考点地图、解释、解题模板和易错点；
2. `题库.json`：四选一练习题、答案和解析；
3. `复习资料.html`：暖米白、陶土橙、单文件离线交互页面；
4. `sources.json`：材料登记与定位方式；
5. `verification.json`：事实、公式和题目的核验状态；
6. `复习资料.pdf`：仅在本机有兼容浏览器且用户需要时生成。

详细格式见 [references/output-spec.md](references/output-spec.md)。

## 工作流

### 1. 建立来源账本

为每份输入材料分配 `S01`、`S02` 等编号，记录文件名、版本、页码类型、覆盖范围和权利说明。不要把用户原始教材、真题、私人笔记或扫描件复制进 Skill 包、公开仓库或其他公共位置。

来源和核验规则见 [references/source-verification.md](references/source-verification.md)。无法定位页码时使用章节、节名、课件页或题号，不得虚构页码。

### 2. 先做覆盖清单，再写正文

从考试范围和材料目录提取考点清单；有真题时统计“样本内出现次数”，同时写明样本数量和年份缺口。只把材料支持的频率称为高频，不把高频等同于必考。

按以下优先级组织：明确考试范围且历年反复出现；明确考试范围但样本较少；教材基础和前置知识；超纲或只有外部常识支持的内容默认不进入主线。

### 3. 分章写作

先读 [references/content-contract.md](references/content-contract.md)，再按章写到 `content/`。每个考点至少包括：

- 一句“这是什么、为什么考”；
- 原定义或公式及来源；
- 大白话解释或具体类比；
- 可执行的解题／作答步骤；
- 一个易错反例；
- 对应练习及解析（材料足够时）。

不要为了完整度补写材料没有的阈值、公式条件、年份、作者观点或题目数据。确需通用补充时显式标记“补充知识”，并与课程依据分开。

### 4. 核验

事实、数字、公式和题目分别核验：

- 事实与公式回查来源位置，符号和适用条件保持一致；
- 每道题检查题干是否自洽、选项是否唯一、答案是否可由题干推出；
- 计算题从空白状态重新解一遍，不直接沿用转写答案；
- 条件允许时用独立上下文做第二次解答；否则在 `verification.json` 标记 `pending_second_pass`，不要宣称已双重核验。

发现原题或答案矛盾时保留原始信息、标记冲突并停止把它当作练习金标准。

### 5. 构建与校验

`<SKILL_DIR>` 指本 Skill 所在目录，`<COURSE_DIR>` 指科目工作目录。

```bash
python <SKILL_DIR>/scripts/init_project.py <COURSE_DIR>
python <SKILL_DIR>/scripts/assemble.py <COURSE_DIR>
python <SKILL_DIR>/scripts/build.py <COURSE_DIR>
python <SKILL_DIR>/scripts/validate_project.py <COURSE_DIR>
```

需要 PDF 时再运行：

```bash
python <SKILL_DIR>/scripts/make_pdf.py <COURSE_DIR>
```

构建脚本只读取本地科目目录，不联网、不读取凭据。PDF 脚本找不到兼容浏览器时保留 HTML，并明确告诉用户如何在浏览器中“打印为 PDF”。

## 质量闸门

只有以下条件都满足才称为“完成”：

- 考试范围内的重要考点已进入覆盖清单；
- 主结论有来源，补充知识已显式区分；
- 题库答案唯一且完成规定的核验；
- HTML 断网可打开，无远程脚本、字体、API 或密钥；
- `validate_project.py` 无 `ERROR`；
- 抽查首页、公式／表格密集页和题卡页，确认无乱码、溢出或打印截断。

若仅生成了部分章节，输出必须写“阶段稿”和未覆盖清单。

## 隐私与版权

- 原材料默认仅在用户指定的本地工作目录处理。
- 不上传教材、试卷、学生信息、密钥或私人笔记。
- 交付件优先写原创总结和必要的短引用，不复制整章教材。
- 用户无权提供或公开的材料只用于本次本地整理，不进入可公开包。

## 代表性例子

请求：“我有《概率论与数理统计》第五版、考试范围和 12 套真题，做零基础期末速通资料。”处理：登记材料和页码体系，统计样本内题型频率，按考点写带来源的精讲与题库，再生成离线 HTML。

请求：“把这些工程流体力学章节和老师重点整理成一周复习包，没有真题。”处理：以老师范围和教材结构排序，不声称题型频率或押题命中率；输出章节讲义、练习和七天计划。

误触发请求：“给香港小六出一套数学期末卷。”这是新试卷生成，不使用本 Skill。

失败输入：“没资料，猜今年原题并保证 90 分。”拒绝保证和伪造依据，可提供通用复习框架，并列出开始制作所需材料。
